### df.isnull.sum()
 - How should we replace the null value?
 - The slution is to choose either mean or mediean to replace it. IN this case median is the better choice as mean can get affected by outliers. Outliers can skew our distribution of data. In whice median is robust to outliers